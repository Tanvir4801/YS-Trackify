{
  "buildCommand": "cd Trackify_Admin && npm install --include=dev && npm run build",
  "outputDirectory": "Trackify_Admin/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
