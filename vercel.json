{
  "buildCommand": "cd Trackify_Admin && npm install && npm run build",
  "outputDirectory": "Trackify_Admin/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
